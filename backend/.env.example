# Database Configuration
# Copy this file to .env and update with your actual database credentials

DB_HOST=localhost
DB_NAME=ffangles
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_PORT=5432

# Example for different setups:
# Local PostgreSQL: DB_HOST=localhost, DB_PORT=5432
# Docker PostgreSQL: DB_HOST=localhost, DB_PORT=5432
# Remote database: DB_HOST=your-db-host.com, DB_PORT=5432